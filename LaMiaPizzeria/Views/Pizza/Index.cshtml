@model List<Pizza>

<div class="container text-center">

    <div class="row my-4">

        <div class="col-12">

            <h1 class="fs-3" style="font-family: 'Dancing Script', cursive;">LE NOSTRE PIZZE</h1>

        </div>

    </div>

    <div class="row my-4">

        <div class="col-12">

            <table class="table bg-warning-subtle shadow-lg rounded">

                <thead>
                    <!--
                    <tr>
                        <th scope="col" class="col-lg-3">Foto</th>
                        <th scope="col" class="col-lg-2">Nome</th>
                        <th scope="col" class="col-lg-6">Descrizione</th>
                        <th scope="col" class="col-lg-1">Prezzo</th>
                        </tr>-->
                    <tr>
                        <th scope="col" class="col-lg-3">Foto</th>
                        <th scope="col" class="col-lg-2">Nome</th>
                        <th scope="col" class="col-lg-1">Prezzo</th>
                        <th scope="col" class="col-lg-6">Opzioni</th>                       
                    </tr>


                </thead>

                <tbody style="font-family: 'Dancing Script', cursive;">

                    @foreach(Pizza pizza in Model)
                    {                   
                           <!-- <tr>
                                <td>
                                    <a class="btn" href="@Url.Action("Dettagli", "Pizza", new { id = pizza.Id })">
                                        <img class="img-fluid" src="@pizza.Immagine" alt="Immagine @pizza.Nome" width="250">
                                    </a>
                                </td>
                                <td class="fs-3"><strong>@pizza.Nome</strong></td>
                                <td>@pizza.Descrizione</td>
                                <td><strong>@pizza.Prezzo €</strong></td>
                            </tr> -->

                        <tr>
                            <td><img class="img-fluid" src="@pizza.Immagine" alt="Immagine @pizza.Nome" width="250"></td>
                            <td class="fs-3"><strong>@pizza.Nome</strong></td>
                            <td><strong>@pizza.Prezzo €</strong></td>
                            <td>
                                <a class="btn btn-dark" href="@Url.Action("Dettagli", "Pizza", new { id = pizza.Id })">Dettagli pizza</a>
                                <a class="btn btn-dark" href="@Url.Action("Update", "Pizza", new { id = pizza.Id })">Modifica</a>
                                <a class="btn btn-dark" href="@Url.Action("Dettagli", "Pizza", new { id = pizza.Id })">Togli dal menù</a>
                            </td>                            
                        </tr>
                    }

                </tbody>

            </table>

        </div>

    </div>

    <div class="row">

        <div class="col-12">      
            
            <a class="btn btn-dark" href="@Url.Action("Create", "Pizza")">
                Aggiungi Pizza
            </a>  

        </div>

    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe" crossorigin="anonymous"></script>

</div>